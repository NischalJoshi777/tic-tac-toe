import 'package:flutter/material.dart';

class LocalRoomScreen extends StatelessWidget {
  const LocalRoomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: const Scaffold(
        body: Column(
          children: [],
        ),
      ),
    );
  }
}
